{
  "name": "RanchiKart",
  "version": "2.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"bun run dev -w frontend\" \"bun run dev -w backend\"",
    "build": "bun run build -w frontend && bun run build -w backend",
    "typecheck": "bun run typecheck -w frontend && bun run typecheck -w backend",
    "db:generate": "bun run db:generate -w backend",
    "db:migrate": "bun run db:migrate -w backend",
    "db:seed": "bun run db:seed -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3"
  }
}
