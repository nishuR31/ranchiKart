============================================================
RANCHIKART DATABASE SCHEMA DOCUMENTATION
============================================================

DATABASE
--------
Provider : PostgreSQL
ORM      : Prisma ORM

============================================================
TABLE : User
============================================================

Primary Key
------------
id

Columns
-------
id                  String (PK, cuid)
email               String (Unique)
name                String (Nullable)
passwordHash        String (Nullable)
role                Role Enum (USER | MANAGER | ADMIN | SELLER) (Default: USER)
avatarUrl           String (Nullable)
gender              String (Nullable)
phone               String (Nullable)
isEmailVerified     Boolean (Default: false)
coins               Integer (Default: 0)
isBanned            Boolean (Default: false)
banReason           String (Nullable)
lastLogin           DateTime (Default: now)
refreshToken        String (Nullable)
totpSecret          String (Nullable)
isTotpEnabled       Boolean (Default: false)
createdAt           DateTime (Default: now)
updatedAt           DateTime (updatedAt)

Relationships
-------------
User
 ├── 1 -----> Many Orders
 ├── 1 -----> Many Reviews
 ├── 1 -----> Many Wishlist Items
 ├── 1 -----> Many Saved Addresses
 ├── 1 -----> Many Passkey Credentials
 └── 1 -----> Many Admin Logs

Indexes
-------
UNIQUE(email)
INDEX(email)

============================================================
TABLE : PasskeyCredential
============================================================

Primary Key
------------
id

Foreign Keys
------------
userId -> User.id

Columns
-------
id                  String (PK, cuid)
userId              String (FK -> User.id, Cascade Delete)
credentialID        String (Unique)
credentialPublicKey Bytes
counter             BigInt
transports          String[] (Array of strings)
createdAt           DateTime (Default: now)
updatedAt           DateTime (updatedAt)

Relationships
-------------
Many Passkeys -----> One User

Indexes
-------
UNIQUE(credentialID)
INDEX(userId)

============================================================
TABLE : Category
============================================================

Primary Key
------------
id

Foreign Keys
------------
parentId -> Category.id (Optional)

Columns
-------
id                  String (PK, cuid)
slug                String (Unique)
name                String
description         String
imageUrl            String
kind                ProductKind Enum (EATABLE | STATIONERY | ELECTRONIC | CLOTHING | SHOE | BAG | ACCESSORY | JEWELLERY | BEAUTY | HEALTH | SPORT | HOME | KITCHEN | GARDEN | PET | BABY | TOY | STAMP | BOARD | OTHER)
parentId            String (Nullable, FK -> Category.id)
createdAt           DateTime (Default: now)
updatedAt           DateTime (updatedAt)

Relationships
-------------
Category
 ├── 1 -----> Many Products
 ├── 1 -----> Many Coupons
 ├── 1 -----> Many Child Categories
 └── Many ---> One Parent Category

Indexes
-------
UNIQUE(slug)
INDEX(slug)
INDEX(parentId)

============================================================
TABLE : Product
============================================================

Primary Key
------------
id

Foreign Keys
------------
categoryId -> Category.id

Columns
-------
id                  String (PK, cuid)
categoryId          String (FK -> Category.id)
slug                String (Unique)
name                String
description         String
kind                ProductKind Enum
imageUrl            String
gallery             Json (Default: "[]")
basePrice           Integer (in paisa)
currency            String (Default: "INR")
stock               Integer (Default: random)
isActive            Boolean (Default: true)
isFeatured          Boolean (Default: false)
tags                Json (Default: "[]")
highlights          Json (Default: "[]")
specifications      Json (Default: "{}")
rating              Float (Default: random)
reviewCount         Integer (Default: 0)
replacementDays     Integer (Default: 7)
returnEligible      Boolean (Default: false)
warrantyText        String (Default: "Manufacturing defects covered as per seller policy.")
dispatchDays        Integer (Default: 3)
minWidthMm          Integer (Nullable)
maxWidthMm          Integer (Nullable)
minHeightMm         Integer (Nullable)
maxHeightMm         Integer (Nullable)
defaultWidthMm      Integer (Nullable)
defaultHeightMm     Integer (Nullable)
options             Json (Default: "{}")
createdAt           DateTime (Default: now)
updatedAt           DateTime (updatedAt)

Relationships
-------------
Many Products ------> One Category

One Product
 ├── Many Variants
 ├── Many Reviews
 ├── Many Wishlist Entries
 └── Many Order Items

Indexes
-------
UNIQUE(slug)
INDEX(slug)
INDEX(categoryId)
INDEX(isActive)
INDEX(isFeatured)

============================================================
TABLE : ProductVariant
============================================================

Primary Key
------------
id

Foreign Keys
------------
productId -> Product.id

Columns
-------
id                  String (PK, cuid)
productId           String (FK -> Product.id, Cascade Delete)
name                String
sku                 String (Unique)
priceDelta          Integer (Default: 0)
attributes          Json (Default: "{}")
stock               Integer (Default: 100)
createdAt           DateTime (Default: now)
updatedAt           DateTime (updatedAt)

Relationships
-------------
Many Variants -----> One Product
One Variant -------> Many OrderItems

Indexes
-------
UNIQUE(sku)
INDEX(productId)

============================================================
TABLE : Review
============================================================

Primary Key
------------
id

Foreign Keys
------------
productId -> Product.id
userId    -> User.id
orderId   -> Order.id (Optional)

Columns
-------
id                  String (PK, cuid)
productId           String (FK -> Product.id, Cascade Delete)
userId              String (FK -> User.id, Cascade Delete)
orderId             String (Nullable, FK -> Order.id, SetNull on Delete)
rating              Integer
title               String (Nullable)
body                String
isVerified          Boolean (Default: false)
helpfulCount        Integer (Default: 0)
createdAt           DateTime (Default: now)
updatedAt           DateTime (updatedAt)

Relationships
-------------
Many Reviews -----> One Product
Many Reviews -----> One User
Optional ---------> One Order

Indexes
-------
UNIQUE(productId,userId)
INDEX(productId)
INDEX(userId)
INDEX(orderId)

============================================================
TABLE : Wishlist
============================================================

Primary Key
------------
id

Foreign Keys
------------
userId -> User.id
productId -> Product.id

Columns
-------
id                  String (PK, cuid)
userId              String (FK -> User.id, Cascade Delete)
productId           String (FK -> Product.id, Cascade Delete)
createdAt           DateTime (Default: now)

Relationships
-------------
Many Wishlist -----> One User
Many Wishlist -----> One Product

Indexes
-------
UNIQUE(userId,productId)
INDEX(userId)

============================================================
TABLE : Coupon
============================================================

Primary Key
------------
id

Foreign Keys
------------
categoryId -> Category.id (Optional)

Columns
-------
id                  String (PK, cuid)
code                String (Unique)
description         String (Nullable)
type                CouponType Enum (PERCENT | FIXED) (Default: PERCENT)
value               Integer
minOrderAmount      Integer (Default: 0)
maxUses             Integer (Nullable)
usedCount           Integer (Default: 0)
isActive            Boolean (Default: true)
expiresAt           DateTime (Nullable)
categoryId          String (Nullable, FK -> Category.id)
createdAt           DateTime (Default: now)
updatedAt           DateTime (updatedAt)

Relationships
-------------
One Coupon ------> Many Orders
Many Coupons ----> One Category

Indexes
-------
UNIQUE(code)
INDEX(code)
INDEX(categoryId)

============================================================
TABLE : SavedAddress
============================================================

Primary Key
------------
id

Foreign Keys
------------
userId -> User.id

Columns
-------
id                  String (PK, cuid)
userId              String (FK -> User.id, Cascade Delete)
label               String (Default: "Home")
fullName            String
phone               String
line1               String
line2               String (Nullable)
city                String
state               String
pincode             String
isDefault           Boolean (Default: false)
createdAt           DateTime (Default: now)
updatedAt           DateTime (updatedAt)

Relationships
-------------
Many Addresses -----> One User

Indexes
-------
INDEX(userId)

============================================================
TABLE : Order
============================================================

Primary Key
------------
id

Foreign Keys
------------
userId -> User.id
couponId -> Coupon.id (Optional)

Columns
-------
id                  String (PK, cuid)
userId              String (FK -> User.id)
status              OrderStatus Enum (PENDING_PAYMENT | PAID | PROCESSING | SHIPPED | DELIVERED | CANCELLED | REFUNDED) (Default: PENDING_PAYMENT)
paymentMethod       PaymentMethod Enum (UPI | CARD | CRYPTO | NET_BANKING | COINS)
subtotal            Integer
shippingFee         Integer (Default: 0)
discountAmount      Integer (Default: 0)
total               Integer
address             Json
trackingId          String (Nullable)
notes               String (Nullable)
couponId            String (Nullable, FK -> Coupon.id)
createdAt           DateTime (Default: now)
updatedAt           DateTime (updatedAt)

Relationships
-------------
Many Orders -----> One User

One Order
 ├── Many OrderItems
 ├── Many Payments
 └── Optional Coupon

Indexes
-------
INDEX(userId)
INDEX(status)
INDEX(createdAt)
INDEX(couponId)

============================================================
TABLE : OrderItem
============================================================

Primary Key
------------
id

Foreign Keys
------------
orderId -> Order.id
productId -> Product.id
variantId -> ProductVariant.id (Optional)

Columns
-------
id                  String (PK, cuid)
orderId             String (FK -> Order.id, Cascade Delete)
productId           String (FK -> Product.id)
variantId           String (Nullable, FK -> ProductVariant.id)
quantity            Integer
unitPrice           Integer
total               Integer
customWidthMm       Integer (Nullable)
customHeightMm      Integer (Nullable)
customText          String (Nullable)
customization       Json (Default: "{}")

Relationships
-------------
Many OrderItems -----> One Order
Many OrderItems -----> One Product
Optional -----------> One Variant

============================================================
TABLE : Payment
============================================================

Primary Key
------------
id

Foreign Keys
------------
orderId -> Order.id

Columns
-------
id                  String (PK, cuid)
orderId             String (FK -> Order.id, Cascade Delete)
provider            PaymentProvider Enum (RAZORPAY)
status              PaymentStatus Enum (CREATED | AUTHORIZED | CAPTURED | FAILED | REFUNDED) (Default: CREATED)
amount              Integer
currency            String (Default: "INR")
providerOrderId     String (Unique)
providerPaymentId   String (Nullable)
providerSignature   String (Nullable)
rawResponse         Json (Nullable)
createdAt           DateTime (Default: now)
updatedAt           DateTime (updatedAt)

Relationships
-------------
Many Payments -----> One Order

Indexes
-------
UNIQUE(providerOrderId)
INDEX(orderId)

============================================================
TABLE : AdminLog
============================================================

Primary Key
------------
id

Foreign Keys
------------
adminId -> User.id

Columns
-------
id                  String (PK, cuid)
adminId             String (FK -> User.id)
action              String
entity              String
entityId            String (Nullable)
meta                Json (Nullable)
createdAt           DateTime (Default: now)

Relationships
-------------
Many Logs -----> One Admin(User)

Indexes
-------
INDEX(adminId)
INDEX(entity)